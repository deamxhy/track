
import React from "react";
import ReactDOM from "react-dom/client";
import TrackRatingApp from "./TrackRatingApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrackRatingApp />
  </React.StrictMode>
);
